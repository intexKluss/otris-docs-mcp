---
title: "Setup using Gitlab as an example"
source: "https://otris.software/documents/manuals/books/otropenid-doc-en/openid/example-gitlab.html"
---

# Setup using Gitlab as an example

![Creating the application](../assets/openid/gitlab-demo-1.png)


| Setting | Value | Note |
| --- | --- | --- |
| Name | otris Software: <Product name> | Example: otris Software: Documents |
| Redirect URI | https://<HOST>/documents<5>/srv/GenericRedirectReceiver | Customize host, documents (Windows and Linux when using documentsOS) or documents5 (Linux when using Documents5) depending on Installation |
| Scope | openid | The openid scope is required, all others can be enabled as needed to e.g. create users in an autologin script. The token generated by the user at login is made available in the autologin script so that further requests can be made to e.g. Gitlab. |

![Creating the application](../assets/openid/gitlab-demo-2.png)


After creation the `Application ID` and the `Secret` must be entered in
the [OpenID Config](../openid/config-openid-config.html). In the case of Gitlab, the `wellKnownOpenIdConfigURL` usually
follows the following pattern:
`<Gitlab URL>/.well-known/openid-configuration`.

For testing, this URL can also be opened in the browser, a JSON structure is expected.

Since there is currently no import of users, they have to be created manually. For this the property `loginAlias` must
be set for each user. The value is the `alias` and the `login_claim` is used from
the [OpenID Config](../openid/config-openid-config.html) according to the following scheme: `alias:login_claim`. The
default (`sub`) is the user ID from gitlab. This can be taken for the user on the respective profile page.

If you want to use a different `login_claim` like e.g. `email` you may have to adjust the `scope` parameter in the
[OpenID Config](../openid/config-openid-config.html). Multiple scopes can be specified, these are separated by *spaces*. The
scope `openid` must always be specified. More detailed information about the possible scopes/claims can also be obtained
by calling the `wellKnownOpenIdConfigURL`.

![Creating the application](../assets/openid/gitlab-demo-3.png)


The first time you log in with your Gitlab account, you still need to confirm the application.

[PDF Version](../openid/example-gitlab.pdf?b=2026-02-12)